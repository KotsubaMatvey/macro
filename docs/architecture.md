# Architecture

The web app renders marketing and workstation views, the API serves deterministic demo-mode endpoints, and the worker refreshes seeded state to keep the product runnable without external providers.
